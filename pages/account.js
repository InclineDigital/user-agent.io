//import Link from 'next/link';
import Layout from '../src/components/Layout.js';

const Account = props => (
  <Layout title="Account">
    <main className="my-4">
      <section className="container">
        <h2>Account</h2>
        <p>Todo: allow user to set/change password & choose custom url</p>
      </section>
    </main>
  </Layout>
);

export default Account;
